apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-data
  annotations:
    longhorn.io/number-of-replicas: "1"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: longhorn
