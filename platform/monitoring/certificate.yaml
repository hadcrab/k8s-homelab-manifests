apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: monitoring-grafana
  namespace: monitoring
spec:
  secretName: monitoring-grafana-tls
  commonName: "grafana.kube"
  dnsNames:
    - grafana.kube
  issuerRef:
    name: local-ca-issuer
    kind: ClusterIssuer
